# Credentials reach the pipeline only through the process environment, so direnv
# is what turns the untracked .env into exported variables for this directory.
watch_file .env

if [ -f .env ]; then
  dotenv .env
else
  log_status "no .env yet: copy .env.example to .env and fill in your API keys"
fi
