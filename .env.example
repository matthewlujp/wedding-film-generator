# Copy to .env and fill in the keys the configured adapters need.
# .env is never read by the pipeline itself; direnv loads it into the shell.

# Vision and narrative adapters named "openai".
OPENAI_API_KEY=

# Vision adapter named "deepseek".
DEEPSEEK_API_KEY=
